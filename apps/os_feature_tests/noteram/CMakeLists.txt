# SPDX-License-Identifier: Apache-2.0

if(CONFIG_BL_OS_FEATURE_TESTS_NOTERAM)
  nuttx_add_application(
    NAME
    ${CONFIG_BL_OS_FEATURE_TESTS_NOTERAM_PROGNAME}
    PRIORITY
    ${CONFIG_BL_OS_FEATURE_TESTS_NOTERAM_PRIORITY}
    STACKSIZE
    ${CONFIG_BL_OS_FEATURE_TESTS_NOTERAM_STACKSIZE}
    MODULE
    ${CONFIG_BL_OS_FEATURE_TESTS_NOTERAM}
    SRCS
    noteram_test.c)
endif()
